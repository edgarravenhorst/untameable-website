const i18nConfig = {
  locales: ["en", "nl"],
  defaultLocale: "nl",
  prefixDefault: false, // Use this to show the default prefix, because it doesn't show that by default
};

module.exports = i18nConfig;
